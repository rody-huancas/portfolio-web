/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['rodyhuancas.vercel.app', 'buyerpersona.com', 'app-datec-erp.netlify.app'],
  },
};

export default nextConfig;
